{
  "name": "root",
  "private": true,
  "scripts": {
    "commit": "cz",
    "bs": "lerna bootstrap && npm install",
    "install:kvaas": "npm install --prefix ./cmd/kvaas",
    "start:kvaas": "node ace serve -watch"
  },
  "devDependencies": {
    "commitizen": "^4.2.1",
    "cz-conventional-changelog": "^3.3.0",
    "lerna": "^3.22.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
